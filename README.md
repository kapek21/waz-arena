# Wąż Arena

Competitive snake 180s (folder `snake`). UI nie używa marki Snake.

```bash
npm install
npm run dev   # http://127.0.0.1:5199/
```

- Turniej: GRAJ 3 MIN — ten sam seed mapy i orbów
- Practice Lab — bez locka BoS
- BoS provider: `provider-backend/` port 43122
